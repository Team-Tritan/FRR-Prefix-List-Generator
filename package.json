{
  "name": "prefix-list-updater",
  "module": "main.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --hot-reload src/main.ts",
    "build": "bun build ./src/main.ts --compile --outfile generate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.7.2"
  }
}
